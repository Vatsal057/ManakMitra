__pycache__/
*.pyc
.env

# Corpus embedding cache is a derived build artifact, not source --
# regenerated by backend/scripts/build_index.py. The translation cache is
# the opposite: it's committed deliberately (see backend/data/cache/translations.json).
backend/data/cache/embeddings.npy
backend/data/cache/embeddings.meta.json

node_modules/
frontend/indian-standards-frontend/dist/
prompts